<div class="cohort-edit-container">
<div class="edit-column">
  <%= form_for @cohort, url:cohortupdate_path(@cohort), html: {method: :put} do |f| %>
  <% if @cohort.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cohort.errors.count, "error") %> prohibited this cohort from being saved:</h2>

      <ul>
      <% @cohort.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :name, "*Cohort Name" %><br>
    <%= f.text_field :name, placeholder: "MIAC1" %>
  </div>
  <div class="field">
    <%= f.label :location, "*Location" %><br>
    <%= f.text_field :location, placeholder: "Miami" %>
  </div>
  <div class="field">
      <%= f.label :graddate, "*Graduation Date" %><br>
      <%= f.date_field :graddate %>
    </div>
  <div class="actions">
    <%= f.submit "update", class: "menu-btn"%>
  </div>
<% end %>
</div>
</div>
