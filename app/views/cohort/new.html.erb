<% provide(:title, "Add New Cohort") %>

<h1>Add New Cohort</h1>

<%= render 'form' %>

<div class="float-right">
  <%= link_to "cancel", cohort_index_path, :class => "menu-btn", :method => :get%>
</div>
